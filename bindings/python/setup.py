from setuptools import setup, find_packages

setup(
    name="uds",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8",
    author="Universal Device SDK Contributors",
    description="Python bindings for Universal Device SDK",
    license="Apache 2.0",
)
