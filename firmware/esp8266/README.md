# UDS Firmware SDK — ESP8266

## Requirements

- ESP8266 Non-OS SDK or RTOS SDK
- xtensa-lx106-elf toolchain

## Build

```bash
cd firmware/esp8266
make
```

## Components

- `driver/uds/` — UDS protocol client for ESP8266
